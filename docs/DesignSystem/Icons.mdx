import { Meta } from '@storybook/blocks';

<Meta title='Design System/Icons'/>

# Icons

The icon system is a reexport of the [Heroicons](https://heroicons.com/) library. The icons are available in two variants: `outline` and `solid`. The icons are available in a variety of sizes and can be used in any context where an SVG can be used.

## Icon Gallery

TBD