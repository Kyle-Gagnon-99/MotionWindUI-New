name: Build and Publish storybook to GitHub Pages

on:
    push:
        branches:
            - 'master'

permissions:
    contents: read
    pages: write
    id-token: write

jobs:
    deploy:
        runs-on: ubuntu-latest
